// TestLoom Domain Enums

enum UserRole {
  ADMIN
  QA_ENGINEER
  VIEWER
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
  PENDING_VERIFICATION
}

enum AccountType {
  PENDING
  PERSONAL
  ORGANIZATION
}

enum OtpPurpose {
  LOGIN
  EMAIL_VERIFICATION
  PASSWORD_RESET
}

enum OwnershipType {
  PERSONAL
  COMPANY
}

enum ScenarioStatus {
  DRAFT
  READY
  DEPRECATED
}

enum InputStrategy {
  DETERMINISTIC_EXACT
  ENVIRONMENT_VARIABLE
  RANDOM_OPTION
  DYNAMIC_AUTO_GENERATE
}

enum AssertionType {
  ASSERT_VISIBLE
  ASSERT_TEXT
  ASSERT_URL
}

enum ExecutionStatus {
  PENDING
  RUNNING
  PASSED
  FAILED
  CANCELLED
}

enum StepResultStatus {
  PASSED
  FAILED
  HEALED
  SKIPPED
}

enum ActionType {
  CLICK
  TYPE
  SELECT
  CHECK
  UNCHECK
  HOVER
  SCROLL
  KEYPRESS
  UPLOAD_FILE
  ASSERT
}

enum IntegrationProvider {
  GITHUB
  SLACK
  EMAIL
  WEBHOOK
}

enum TriggerType {
  MANUAL
  SCHEDULED
  CI_CD
  WEBHOOK
}
